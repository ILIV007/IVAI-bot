{
  "name": "ivai-bot",
  "version": "3.3.1",
  "private": true,
  "type": "module",
  "description": "IVAI — privacy-aware, free-tier Telegram AI assistant for Cloudflare Workers.",
  "scripts": {
    "check": "node --check src/*.js",
    "test": "node --test",
    "validate": "npm run check && npm test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=22"
  }
}
